# قائمة المشاكل والمهام (Issues & Tasks)

> **جرد كامل لكل بند — 25 سبتمبر 2026.** كل بند راجعناه في الأكواد الفعلية، لا
> فيما كان مكتوباً هنا عن نفسه. ملاحظة مهمة: الجملة القديمة التي تقول إن بيئة
> التنفيذ لا يوجد بها Flutter SDK كانت **خاطئة**؛ الجهاز عليه Flutter 3.47.5،
> فتم تشغيل كل شيء تشغيلاً حقيقياً.
>
> البوابة وقت الكتابة:
>
> ```
> flutter analyze --no-pub   ->  31 ملاحظة (صفر تحذيرات، صفر أخطاء)
> flutter test  --no-pub     ->  284/284 اختبار ناجح (100%)
> ```
>
> النسبة المئوية لكل بند تجيب على سؤال واحد: كم من ادعاء البند نفسه صحيح في
> الكود الآن؟
>
> | البند | كان معلَّماً | الصحيح فعلاً قبل الجرد | الآن |
> |---|---|---|---|
> | ودجت عرض الوجبة المخصصة (QuickMealView / MealScreen) | ❌ لم تنفذ | 60 % (نسختان متكررتان من الكارت) | 100 % |
> | نافذة تفاصيل الأكلة الموحدة | ✅ | 100 % | 100 % |
> | حماية التعديلات غير المحفوظة | ✅ | 55 % (الحارس موجود لكنه لا يعمل) | 100 % |
> | ثغرات المغادرة في `QuickAddSheet` | ❌ | 33 % (واحدة من ثلاثة) | 100 % |
> | تصدير واقتراح الأكلات للسحابة | ❌ لم تنفذ | 90 % (الضابط الرابع ناقص) | 100 % |
> | عداد تبويب الاستكشاف | ✅ | 100 % | 100 % |
> | سطر الوصف وهيدر الخزنة | ✅ | 90 % (ينقص عند تكبير النص) | 100 % |
> | تحديث اقتراحات الرئيسية | ✅ | 100 % | 100 % |
> | تثبيت الكروت عند الإعجاب | ✅ | 90 % | 100 % |
> | سلوك زر الرجوع الموحد | ⏳ | 70 % (آلة حالة خاطئة) | 100 % (الصياغة حُسمت) |
> | إشعار «اضغط مرة أخرى للخروج» | ⏳ | عطل حقيقي | 100 % |
>
> **تنظيم الملف:** هذا الملف هو مرجع المهام بالعربي (حسب GEMINI.md)، ومعه نسخة
> إنجليزية تفصيلية في `app_v2/ISSUES.md` بنفس البنود مع مؤشرات الأدلة على الملفات
> والأسطر. أي بند يُغلق هنا يُغلق هناك في نفس الجلسة.

## 📱 التصميم وعناصر الواجهة (UI & Layout Components)

- [x] **بناء ودجت/شاشة عرض الوجبة المخصصة (Dedicated Meal Screen / QuickMealView Integration)**:
  - المشكلة/المتطلب: الرغبة في بناء شاشة أساسية جديدة باسم meal_screen لا تعمل كصفحة كاملة مستقلة (Standalone Page) بل كودجت/شاشة مخصصة قابلة للاستدعاء والتضمين بمرونة في مختلف سياقات التطبيق، مع تمييز ودجت كارت الصفحة الرئيسية القديم وتغيير اسمه من MealCard إلى QuickMealView (quick_mealview.dart).
  - المطلوب:
    1. **إعادة تسمية كارت الرئيسية (MealCard -> QuickMealView)**:
       - إعادة تسمية الملف meal_card.dart ليصبح quick_mealview.dart داخل lib/features/home/presentation/widgets/.
       - تغيير اسم الكلاس إلى QuickMealView ليعبر بدقة عن كونه كارت العرض السريع للاقتراحات في شاشة الهوم.
       - تحديث جميع أماكن الاستيراد والاستخدام في home_screen.dart و meal_vault_card.dart.
    2. **بناء الشاشة الأساسية الجديدة (MealScreen)**:
       - تصميم وبناء MealScreen (أو meal_screen.dart) لتكون ودجت عرض متكاملة للأكلة، قابلة للاستدعاء والعرض حسب تخطيط الشاشات المستقبلية.
       - فصل منطق وعرض تفاصيل الوجبة الكاملة بحيث تدعم التكامل السلس مستقبلاً عند استدعائها داخل أي جزء من التطبيق.
  > **الوصف التقني:**
  > 1. نقل وتعديل MealCard إلى QuickMealView وتحديث PROJECT.md ومسارات الاستيراد.
  > 2. إنشاء هيكل MealScreen في المكان المناسب (ضمن lib/features/home/presentation/ أو موديول الوجبات) وتجهيزها لاستقبال معرّف الأكلة وتفاصيلها دون فرض مسار توجيه صلب (Flexible Embeddable View).
  > **الحالة:** ✅ منفذة بالكامل (كانت ❌ ولم تكن كذلك فعلاً: 60 % فقط). ودجت العرض
  > `QuickMealView` كانت موجودة فعلاً في `lib/features/meals/presentation/quick_meal_view.dart`
  > لكن كارت الرئيسية بقي ودجت مستقلة باسم `MealCard` في ملفه القديم، فالنتيجة كانت
  > نسختين متكررتين من نفس فكرة كارت الأكلة في التطبيق.
  > **ما تم في جرد 25 سبتمبر:** أُضيف لـ `QuickMealView` شكلان عبر
  > `enum MealViewShape { detail, quick }` مع حقول `isFavorite` و `onTap` و
  > `onToggleFavorite` و `footer`، ونُقل تخطيط كارت الرئيسية كما هو داخل الشكل
  > `quick` (شريط الصورة العريض، زر الحب العائم بحركة 250 مللي ثانية، شرائط
  > البروتين/الوقت/التصنيف، شارات الجمعة/اقتصادي/مفضلة، ثم التذييل)، وحُذف ملف
  > `lib/features/home/presentation/widgets/meal_card.dart` من الشجرة نهائياً (لا
  > fallback)، و`home_screen.dart` يبني الآن `QuickMealView.fromMeal(shape: quick,
  > footer: QuickActions(...))`، و`meal_vault_card.dart` يستورد الودجت الناجية.
  > **انحراف عن نص البند:** الملف الباقي هو `meals/presentation/quick_meal_view.dart`
  > وليس `home/presentation/widgets/quick_mealview.dart` الذي اقترحه البند؛ إنشاءه
  > تحت home كان سيعيد نفس الازدواج الذي يوجد هذا البند لقتله. مسار `/meal/:id` صار
  > مستهلكاً واحداً لـ `MealScreen` لا شرطاً لها: اختبار `meal_screen_test.dart:100`
  > يضخ `MealScreen(mealId:)` داخل `MaterialApp` بدون أي GoRouter في الشجرة.
  > **التحقق البصري:** رُسمت شاشة الرئيسية فعلاً قبل وبعد التغيير على مقاس 360×800
  > بالعربي مع خط Cairo المحمّل، والصورتان متطابقتان بايت ببايت
  > (`cmp` = identical)، مع `test/widget/quick_meal_view_test.dart` (6 اختبارات
  > منها 3 للشكل السريع).


- [x] **نافذة تفاصيل الأكلة الموحدة متعددة الاستخدامات (Adaptive Meal Details Sheet)**:
  - المشكلة: لا توجد حالياً نافذة مخصصة لاستعراض تفاصيل الأكلة كاملة عند النقر عليها في السجل أو الاستكشاف أو الخزنة، كما أن النقر في كروت الخزنة يفتح التعديل مباشرة دون استعراض التفاصيل أولاً.
  - المطلوب: تصميم نافذة سفلية منبثقة موحدة وعصرية (Modal Bottom Sheet) لعرض تفاصيل الأكلة، تتكيف عناصرها وأزرار التحكم فيها بمرونة حسب الشاشة والتبويب الذي فُتحت منه:
    1. **الشكل الأساسي (Base Info)**:
       - صورة الأكلة بحجم عريض وواضح بأعلى النافذة.
       - اسم الأكلة بخط كبير وواضح.
       - شارات المعلومات الغذائية (نوع البروتين مع الإيموجي، نوع الكارب، وقت التحضير بالدقائق، شارة اقتصادي إن وجد).
    2. **حالة صفحة السجل (History Page)**:
       - تُفتح عند النقر على أي وجبة في خط السجل الزمني.
       - تعرض **الشكل الأساسي فقط** مع تفاصيل تاريخ ويوم طبخها، بدون أي أزرار تعديل أو تنزيل.
    3. **حالة صفحة الخزنة - تبويب الاستكشاف (Explore Tab)**:
       - تُفتح عند النقر على أي وجبة سحابية.
       - تعرض **الشكل الأساسي + زر التنزيل (Download / Save to Vault)** لحفظ الأكلة في الخزانة المحلية، مع توضيح حالة الأكلة إذا كانت محفوظة بالفعل مسبقاً.
    4. **حالة صفحة الخزنة - تبويب خزانتي (My Vault Tab)**:
       - تُفتح عند النقر على أي وجبة محلية.
       - تعرض **الشكل الأساسي + زري الإجراءات (Action Buttons)**:
         - **زر التحديث / التعديل (Edit / Update)**: يفتح نافذة تعديل بيانات الأكلة.
         - **زر الحذف (Delete)**: يفتح نافذة تأكيد حذف الأكلة من الخزانة.
  > **الوصف التقني:**
  > 1. إنشاء ويدجت موحدة `MealDetailsSheet` تدعم استقبال `Meal` محلي أو `CloudMeal` سحابي أو `MealHistory`، مع نمط تشغيل يحدد السياق (`MealDetailsContext.history` / `MealDetailsContext.explore` / `MealDetailsContext.vault`).
  > 2. ربط حدث النقر `onTap` في عناصر السجل `history_screen.dart` لفتح النافذة.
  > 3. ربط حدث النقر `onTap` في كروت الاستكشاف `_CloudMealCard` و `_CloudMealListTile` داخل `discovery_screen.dart` لفتح النافذة مع زر التنزيل.
  > 4. تعديل حدث النقر `onTap` في `meal_vault_card.dart` لفتح نافذة التفاصيل، وتمرير callbacks للتحديث والحذف.
  > 5. الالتزام الصارم بقاعدة الترجمة ونقل كافة التسميات والأزرار لملف `app_strings.dart`.
  > **الحالة:** ✅ منفذة بالفعل (كانت مُعلَّمة `[x]` مع حالة «لم تنفذ» متناقضة). الودجت موجودة كاملة في `lib/features/vault/presentation/widgets/meal_details_sheet.dart` بـ `MealDetailsContext` الثلاثة (history / explore / vault)، ومرتبطة بالنقر في `history_screen.dart` و `discovery_screen.dart` (`_CloudMealCard` و `_CloudMealListTile`) و `meal_vault_card.dart`. النقص الحقيقي كان في الصفحة الرئيسية فقط وتم في نفس الجلسة: انظر بند «ربط النقر على كروت الرئيسية».

---

## 🧭 شريط التنقل ودورة حياة الشاشات (Navigation & Screen Lifecycle)

- [x] **سلوك زر الرجوع الموحد للميزات المستقبلية (Global Back Button Behavior for Future Features)**:
  - **المشكلة**: أي ميزة جديدة سيتم إضافتها (سواء كانت صفحة كاملة أو نافذة منبثقة/Popup) يجب أن تتبع قاعدة موحدة عند الضغط على زر الرجوع.
  - **الحل**:
    1. عند الضغط على زر الرجوع من داخل أي ميزة/صفحة فرعية، يجب أن يعود المستخدم للصفحة الأساسية التي تحتوي على هذه الميزة (أو للتبويبة الرئيسية).
    2. عند الضغط على زر الرجوع أثناء التواجد في الشاشة الرئيسية (Home)، يجب إظهار إشعار (Toast) يطلب من المستخدم "الضغط مرة أخرى للخروج".
    3. إذا كان المستخدم في تبويبة أخرى غير الرئيسية (مثل Vault أو Settings)، أول ضغطة لزر الرجوع تعيده للتبويبة الرئيسية، مع إظهار نفس الإشعار.
  > **ملاحظات فنية:**
  > 1. تم تأسيس الـ PopScope على مستوى الـ ScaffoldWithNavBar ليعالج هذا السلوك، ولكن يجب التأكد من تطبيقه بشكل سليم لأي Navigator داخلي جديد أو Dialog يتم إنشاؤه مستقبلاً لتجنب أي سلوك غير متوقع.
  > 2. يجب عدم الاعتماد على Navigator.pop في الروت بدون التحقق من توافر الشروط السابقة.
  > **الحالة:** ✅ منفذة بالكامل — آلة الحالة صُححت والصياغة حُسم قرارها.
  > **تم في جرد 25 سبتمبر:** الفرع الذي ينقل المستخدم من تبويبة ثانوية إلى الرئيسية
  > كان يُظهر إشعار «اضغط مرة أخرى للخروج» ويُسلّح المؤقّت في نفس اللحظة، فالضغطة
  > التالية تخرج من التطبيق بلا تحذير حقيقي وهو على الرئيسية. الآن التبديل إلى
  > الرئيسية لا يُظهر الإشعار ولا يسلّح شيئاً (`app_router.dart`:275-285)، والتسليح
  > لا يحدث إلا من ضغطة تقع **وهو على الرئيسية** (`:287-297`)، وأي تبديل تبويبة يُلغي
  > التحذير القديم حتى لا يبقى سلاحاً زمنياً فاسداً (`:215`، `:229` — والمسح يتم قبل
  > الخروج المبكر بسبب `_navLock` حتى لا يترك نقرة سريعة مُسقطة تحذيراً عالقاً).
  > نافذة الضغطتين أُخرجت لثابت `_exitWindow` بنفس length السابق، ونص الإشعار لم
  > يتغير. التغطية: `test/widget/global_back_button_test.dart` (6 اختبارات، منها
  > واحد يفشل عند حذف سطر إعادة الضبط).
  > **القرار (25 سبتمبر، حُسم باختيار المستخدم):** يبقى السلوك الحالي — بلا إشعار
  > أثناء الانتقال بين التبويبات، والإشعار فقط عند ضغطة رجوع واقعة على الرئيسية
  > نفسها. وبهذا يُعتبر سطر 57 أعلى هذا البند منسوخاً: كان يطلب ظهور الإشعار عند
  > الانتقال من تبويبة، وهو ما يشكو بند «الإشعار المضلّل» أدناه منه.

- [x] **حماية التعديلات غير المحفوظة وإظهار رسالة تأكيد عند المغادرة (Unsaved Changes Confirmation Dialog)**:
  - المشكلة: عند فتح أي نافذة تعديل تحتوي على زر تأكيد/حفظ (مثل نافذة إضافة أو تعديل الوجبة `QuickAddSheet` أو نافذة تعديل الملف الشخصي `ProfileEditDialog`)، والبدء في تعديل البيانات، فإن الضغط على زر الرجوع أو النقر خارج النافذة أو محاولة الانتقال لصفحة أخرى يقوم بإغلاق النافذة بصمت وفقدان ما كتبه المستخدم دون أي تحذير.
  - المطلوب:
    1. تفعيل فحص التعديلات (`isDirty / hasUnsavedChanges`): التحقق مما إذا كان المستخدم قد عدل أي حقل أو رفع صورة جديدة تختلف عن البيانات الأصلية.
    2. عند محاولة الخروج أو التبديل لصفحة أخرى بوجود تعديلات غير محفوظة، يُمنع الخروج التلقائي وتظهر نافذة حوارية تأكيدية (Confirmation Dialog):
       - **العنوان**: "تجاهل التعديلات؟" (Discard Changes?).
       - **الرسالة**: "لديك تغييرات غير محفوظة، هل أنت متأكد من رغبتك في المغادرة وتجاهل ما قمت بتعديله؟".
       - **الخيارات**:
         - زر أساسي: "متابعة التعديل" (Keep Editing) للبقاء في النافذة وحفظ التعديل.
         - زر ثانوي: "تجاهل التغييرات" (Discard) لتأكيد الخروج وإلغاء التعديلات.
    3. إذا لم يقم المستخدم بإجراء أي تعديل (`!hasUnsavedChanges`)، يُسمح بالخروج والانتقال مباشرة وسلساً دون إظهار أي رسالة.
  > **الوصف التقني:**
  > 1. تغليف نوافذ التعديل (مثل `QuickAddSheet` و `ProfileEditDialog`) بـ `PopScope(canPop: !hasChanges, onPopInvokedWithResult: ...)` المتوافقة مع أحدث إصدارات Flutter.
  > 2. بناء دالة مساعدة موحدة `showDiscardChangesDialog(BuildContext context)` لإعادة استخدام نفس تصميم الحوار في كافة نوافذ التعديل.
  > 3. إضافة مفاتيح النصوص اللازمة للحوار في `app_strings.dart` باللغتين العربية والإنجليزية.
  > **الحالة:** ✅ منفذة، مع إصلاح خلل حرج. البنية كانت موجودة فعلاً (`PopScope` + `hasUnsavedChanges` + الحوار الموحّد `discard_changes_dialog.dart` + المفاتيح الأربعة بالعربيتين)، لكن قيم الإرجاع كانت معكوسة: زر «تجاهل التغييرات» يُرجع `false` وزر «متابعة التعديل» يُرجع `true`، بينما يفسّر المستدعيان `true` على أنه تجاهل — أي أن «متابعة التعديل» كانت ترمي التعديلات و«تجاهل» لا تفعل شيئاً. تم تصحيح القيم في `lib/core/widgets/discard_changes_dialog.dart`. بقيت ثغرتان مغادرة تتجاوزان `PopScope` مسجّلتان كبند مستقل أدناه.
  > **تحديث جرد 25 سبتمبر (البند كان 55 % لا 100 %):** تصحيح القيم المعكوسة وحده لم
  > يكن كفيلاً بأن يعمل الحارس. العطل الحقيقي أن `canPop` كانت تُحسب وقت البناء فقط،
  > ومُشغّل النص لا يعيد بناء الودجت الحاضنة، فكتابة الاسم وحدها لا تجعل النموذج
  > «وسخاً» من منظور الحارس: زر الرجوع كان **يغلق السطح بصمت ويمحو المكتوب** دون أن
  > يظهر الحوار أصلاً. أثبت اختباران مكتوبان ضد الكود القديم هذا بالفشل، ثم مرّا
  > بإضافة `addListener` على الحقول في `profile_edit_dialog.dart` و
  > `quick_add_sheet.dart` (نمط `onFormChanged` + `_closeSelf`).
  > **وما أُضيف أيضاً:** لا كان موجوداً علم صريح يعلن «السطح يُغلق نفسه»، فكان ضغط
  > رجوع أثناء الحفظ أو أثناء حركة الإغلاق يستطيع أن يفتح حوار «تجاهل» فوق عمل
  > حُفظ فعلاً، والنقرة الثانية على زر الحفظ كانت ستخزّن الأكلة مرتين؛ أُضيفت علوم
  > `_saving` / `_isPopping` / `_confirmOpen` في السطحين. وحُما سطحان كانا بلا أي
  > حارس: `welcome_screen.dart` (الرجوع من الخطوة الثانية يرجع للأولى مع الحفاظ على
  > المكتوب، والرجوع في الأولى ببيانات يسأل قبل مغادرة التطبيق، والإرسال الناجح يحرّر
  > الحارس قبل استبدال المسار). **وتركتُ عمداً** `cooldown_details_sheet.dart`: كل
  > مفتاح فيها يُكتب في قاعدة البيانات فوراً ويُستعاد عند إعادة الفتح — لا يوجد
  > «غير محفوظ» أصلاً، وحوار تجاهل هناك يكذب على المستخدم. التغطية الآن:
  > `test/widget/unsaved_changes_guard_test.dart` (10 سيناريوهات) و
  > `test/widget/settings_wifi_only_switch_test.dart` (2).

---

## 🔍 شاشة الخزنة (Vault Page)

- [x] **ميزة تصدير واقتراح الأكلات المحلية إلى الخزنة السحابية (Export / Propose Meal to Cloud Staging)**:
  - **المشكلة/المتطلب**: لا يوجد حالياً أي زر أو لوجيك في تطبيق الموبايل يتيح للمستخدم مشاركة أو اقتراح وجبة محلية قام بإضافتها لرفعها إلى السحابة، بالرغم من أن لوحة تحكم الإدارة وقواعد أمان Firestore مهيأة بالفعل لاستقبال ومراجعة هذه الوجبات في مجموعة `staging_meals` وتبويب المقترحات.
  - **المطلوب**:
    1. **واجهة المستخدم وأماكن الاستدعاء (UI Entry Points)**:
       - إضافة زر واضح وأنيق لاقتراح الأكلة (أيقونة سحابة صاعدة `Icons.cloud_upload_rounded` أو `AppGlyph.cloudUpload`) داخل نافذة تفاصيل الوجبة `MealDetailsSheet` (في حالة الخزنة `MealDetailsContext.vault`) بجانب زري التعديل والحذف.
       - تمييز حالة الأكلة المقترحة: إذا تم اقتراح الأكلة مسبقاً، يتحول الزر إلى حالة غير مفعّلة تحمل شارة (تم الاقتراح ⏳ / `strings.mealProposed`) لمنع تكرار الإرسال بالخطأ.
    2. **ضوابط وقيود الرفع والأمان (Upload Limits & Anti-Spam Safeguards)**:
       - **حد معدل الإرسال (Rate Limiting)**: وضع حد أقصى للاقتراحات المسموحة لكل مستخدم (مثلاً: 3 إلى 5 مقترحات يومياً) لتفادي إغراق السحابة بمقترحات عشوائية أو إساءة الاستخدام.
       - **ضغط وتحجيم الصور (Image Compression & Downscaling)**: منع رفع الصور الأصلية الكبيرة (5-15MB) من كاميرا الموبايل؛ يجب ضغط الصورة تلقائياً ليكون حجمها الأقصى بين `300KB - 500KB`، وبأبعاد قصوى `1080×1080` بكسل قبل الرفع لتقليل استهلاك باقة المستخدم ومساحة التخزين السحابي.
       - **فحص التكرار المسبق (Duplicate Check)**: إجراء مطابقة لاسم الأكلة مع الخزنة العامة قبل الإرسال؛ إذا كانت الوجبة معتمدة وموجودة بالفعل يُنبه المستخدم فوراً: *"هذه الأكلة متوفرة بالفعل في الخزنة العامة 👏"* دون استهلاك عملية رفع.
       - **التحقق من صحة واكتمال البيانات (Data Validation)**: التأكد من اكتمال الحقول الأساسية (اسم الأكلة لا يقل عن حرفين، نوع البروتين، نوع النشويات، ووقت التحضير) لتتوافق مع معايير الأمان وقواعد Firestore (`isValidStagingMeal`).
    3. **بناء لوجيك الرفع والخدمة السحابية (Logic & Service Integration)**:
       - إنشاء خدمة مخصصة `MealProposalService` في موديول الخزنة تقوم برفع الصورة إلى التخزين السحابي (Cloudinary أو Firebase Storage)، ثم إنشاء وثيقة في مجموعة `staging_meals` بحالة `status: 'pending'`.
       - إظهار مؤشر تقدم خفيف (Toast / SnackBar) يُعلم المستخدم بنجاح الاقتراح: *"تم إرسال اقتراحك للمراجعة بنجاح! شكراً لمشاركتك 🧡"*.
  > **الوصف التقني:**
  > 1. إضافة حقل لتتبع حالة الاقتراح محلياً في جدول `meals` (مثل `is_proposed` أو الاحتفاظ بسجل الاقتراحات في `SharedPreferences`).
  > 2. دمج زر الاقتراح في `MealDetailsSheet` وضبط الصلاحيات بحيث يظهر فقط للأكلات المحلية التي لم تُقترح بعد وليست من الأكلات التأسيسية (`!isStarterMeal`).
  > 3. إنشاء دالة `proposeMeal(Meal meal)` تتولى ضغط الصورة عبر حزمة معالجة الصور، والاتصال بمجموعة `staging_meals` مع تضمين معرّف المستخدم المجهول/المسجل `proposedBy`.
  > 4. إضافة نصوص ومفاتيح الترجمة الكاملة في `app_strings.dart` باللغتين العربية والإنجليزية.
  > **الحالة:** ✅ منفذة بالكامل (كانت معلَّمة ❌ وهي 90 % منفذة — التعليمة قديمة).
  > **ما هو موجود ويُعمل به:** زر الاقتراح في `MealDetailsSheet` بسياق الخزنة + رابط
  > الشاشة الكاملة، و`MealProposalService` يبني حمولة مطابقة سطراً بسطر لـ
  > `firestore.rules::isValidStagingMeal` (تمت مقارنة كل حقل: الاسم 2-100، وقت الطهي
  > 5-720، الملاحظات ≤500، `imageUrl` ≤2048، `isStarterMeal == false`،
  > `status: 'pending'`، `createdAt` بصيغة ISO، `proposedBy == uid`، وقيم
  > التصنيف/البروتين/الكربوهيدرات داخل المجموعات التي تقبلها القواعد) ويكتب في
  > `staging_meals` بعد تسجيل دخول مجهول، و**حدّ المعدل**
  > `ProposalQuota.dailyLimit = 5` في اليوم لكل جهاز، و**ضغط الصور**: `image_picker`
  > يلتقط بـ `maxWidth: 1080` وجودة 85 والبوابة عند 500 كيلوبايت قبل الرفع، و**فحص
  > التكرار المحلي** عبر دفتر `SharedPreferences` الذي يقارن `updatedAt`، و**نص
  > الرفع** صار إلى Cloudinary (مسار التخزين القديم مُتقاعد)، والتغطية
  > `test/unit/meal_proposal_payload_test.dart` (77 اختباراً).
  > **الفجوة الرابعة التي كانت ناقصة وأُغلقت في الجرد:** المطابقة مع **الخزنة العامة**
  > قبل الإرسال. لم يكن لها أثر في الكود (لا ذكر لـ `vault_meals` داخل الخدمة)، فكان
  > المستخدم يقترح أكلة موجودة فعلاً في الخزنة العامة، فيستهلك رفعة صورة ورصيد اليوم
  > ويرمي نسخة مكررة في طابور مراجعة الأدمن. الآن يوجد فحص استطلاعي قبل تسجيل الدخول
  > وقبل الرفع وقبل استهلاك الرصيد: `MealProposalPayload.vaultProbeNames` يعيد نفس
  > القيمة المطبّعة التي تُكتب في حقل `name` (trim + قصّ إلى 100) ويزيد `shortName`
  > إن وجد، ثم `where('name', isEqualTo:).limit(1)` على `vault_meals` — القراءة متاحة
  > بلا جلسة لأن القواعد تعطيها `allow read: if true` — وتظهر النتيجة تحت حدّ
  > مستقل باسم `alreadyInPublicVault` بالنص المطلوب حرفياً «هذه الأكلة متوفرة بالفعل في الخزنة
  > العامة 👏»، بلا استهلاك رصيد وبلا تسجيل في الدفتر المحلي فيُمكنه اقتراح أكلة أخرى
  > فوراً. الفحص محدد بـ 8 ثوانٍ وأي فشل فيه يُبتلع ويستمر الاقتراح: فاشلٌ ألا يُحجب
  > اقتراح مشروع بسبب فحص مكررات معطوب.
  > **قيود معلنة في الكود:** مطابقة السلاسل في Firestore حساسة للحركة والتشكيل، فلا
  > تُلتقط الأسماء القريبة؛ تُترك للأدمن عند الفرز (بلا مطابقة ضبابية وبلا جلب
  > المجموعة كاملة). وملاحظة أمان تستحق قراراً: preset الرفع في Cloudinary غير موقّع
  > والحد اليومي محلي فقط، فالمنع الحقيقي يحتاج قيوداً على الـ preset أو دالة
  > Firebase Function.


- [x] **تعديل إحصائيات عداد تبويب الاستكشاف (استبدال المشترك بالجديد)**:
  - المشكلة: في هيدر تبويب "استكشاف" بشاشة الخزنة، يتم حالياً عرض إحصائية الأكلات المشتركة (`vaultSharedCount`) بجوار إجمالي أكلات السحابة، وتحتها سطر إضافي للأكلات الجديدة (`vaultNewCount`).
  - المطلوب:
    1. إزالة إحصائية الأكلات المشتركة نهائياً من العداد.
    2. وضع عدد الأكلات **"الجديدة"** مكان المشتركة مباشرة بجوار إجمالي أكلات السحابة في سطر واحد أنيق (مثال: `[ 45 جديدة ✨  |  65 أكلة ☁️ ]`)، دون الحاجة لسطر رأسي إضافي.
  > **الوصف التقني:**
  > 1. في `meal_vault_screen.dart` بدالة `_buildVaultCounter` داخل قسم `isExplore`:
  >    - حذف ويدجت عرض `vaultSharedCount`.
  >    - وضع شارة `vaultNewCount(newCount)` في الـ `Row` بجوار شارة `vaultCloudCount(cloudCount)`.
  >    - إزالة السطر السفلي الإضافي وتحويل العداد لسطر أفقي وحيد لتقليل الارتفاع الرأسي.
  > **الحالة:** ✅ منفذة. حُذفت شارة `vaultSharedCount` (وتم حذف مفتاح الترجمة نفسه من `app_strings.dart` لأنه لم يعد مستخدماً في أي مكان)، وأصبحت شارة `vaultNewCount` تلي `vaultCloudCount` في `Row` أفقي وحيد. حساب `sharedCount` بقي ضرورياً لأنه مصدر اشتقاق `newCount`.

- [x] **استعادة ظهور سطر الوصف وإصلاح تخطيط الهيدر وأيقونة المزامنة (Restore Vault Subtitle & Header Layout)**:
  - المشكلة: سطر الوصف التوضيحي في هيدر الخزنة ("احفظ أكلاتك المفضلة واطبخها في أي وقت 🧡" / `strings.vaultSubtitle`) مختفٍ تماماً ولا يظهر في تبويب خزانتي. السبب يرجع لوضع أيقونة المزامنة في عمود رأسي أسفل العداد مما جعل الجزء الأيمن طويلاً، ومع تحديد `toolbarHeight: 85` في الـ `SliverAppBar`، تم اقتطاع سطر الوصف ودفعه خارج حدود العرض.
  - المطلوب:
    1. استعادة سطر الوصف وإظهاره بشكل كامل وواضح ومريح للعين كما كان في الأصل.
    2. إعادة تنظيم التخطيط بحيث لا تكون أيقونة المزامنة في سطر منفصل يزاحم الوصف أو يُخفيه: إعادتها لتكون في نفس السطر بشكل مدمج ومتناسق كما كانت في السابق دون التسبب في إخفاء الوصف.
    3. ضبط ارتفاع الـ `SliverAppBar` (`toolbarHeight` / `flexibleSpace`) ليتناسب تماماً مع المحتوى ويمنع أي قص (Clipping) لسطر الوصف في مختلف أحجام الشاشات.
  > **الوصف التقني:**
  > 1. في `_buildVaultHeaderWidget` بملف `meal_vault_screen.dart`، إعادة هيكلة توزيع العنوان والوصف وأيقونة المزامنة والعداد.
  > 2. دمج أيقونة المزامنة أفقياً بجوار العداد أو مع سطر الوصف بشكل خفيف وأنيق بدلاً من وضعها كعنصر رأسي منفصل يزيد ارتفاع الهيدر.
  > 3. في `_buildMyVaultTab`، ضبط `toolbarHeight` في الـ `SliverAppBar` للقيمة المناسبة (مثلاً 100-110 أو استخدام `SliverToBoxAdapter` مرن) لضمان ظهور سطر الوصف بوضوح تام تحت العنوان.
  > **الحالة:** ✅ منفذة. السبب كان هندسياً بحتاً: عمود العداد (شارة + مسافة 6 + أيقونة المزامنة) كان يرتفع ~58 بكسل في مقابل `toolbarHeight: 85`، فيحتاج الهيدر ~101 بكسل ويُقتطع سطر الوصف. أصبح `_buildVaultCounter` في تبويب «خزانتي» `Row` أفقي بدل `Column`، فبقي `toolbarHeight` على 85 دون تغيير.

---

## 🧠 محرك الاقتراحات والتهدئة (Recommendation & Cooldown Engine)
- [x] **إصلاح ميزة تحديث وتغيير اقتراحات الوجبات الثلاث بالرئيسية (Fix Recommendations Refresh & Shuffling)**:
  - المشكلة: ميزة تغيير وتحديث اقتراحات الوجبات في الصفحة الرئيسية لا تعمل إطلاقاً؛ فعند سحب الشاشة للتحديث (Pull-to-refresh) والموافقة على التحديث، تظل الوجبات الثلاث المقترحة ثابتة ومطابقة تماماً دون أي تغيير مهما كرر المستخدم المحاولة.
  - السبب التقني:
    1. في `CooldownEngine.calculateMealScore`، تعتمد الخوارزمية على معادلة نقاط تهيمن عليها أوزان الحداثة والمفضلة (`sRecency = 25.0` و `sFavorite = 5.0`)، بينما قيمة التوزيع العشوائي (`jitter`) صغيرة جداً (بحد أقصى `3.96`)، مما يجعل نفس الوجبات ذات النقاط الأعلى تتصدر دائماً دون أن يؤثر تغير الـ `shuffleSeed` إطلاقاً.
    2. في `_rankAndSelectDiversity`، يتم أخذ أول عنصر دائمًا ثم البحث عن تنوع البروتين من بين العناصر الأولى ذات النقاط الأعلى، وبالتالي لا تتاح أي فرصة للوجبات الأخرى في الخزانة للظهور كبدائل عند التحديث.
  - المطلوب:
    1. جعل التحديث يغير الوجبات فعلياً: عند قيام المستخدم بتحديث الاقتراحات، يجب أن تظهر له 3 وجبات بديلة وجديدة من الخزانة من بين الوجبات المؤهلة (التي لا تخضع لفترة التهدئة).
    2. تطبيق خلط عشوائي حقيقي ومؤثر (`Random(seed).shuffle()` أو منح الـ `shuffleSeed` وزناً حاسماً) بين كافة المرشحين المؤهلين على نفس المستوى قبل اختيار الوجبات الثلاث المتنوعة بروتينياً.
    3. الحفاظ على ثبات الاقتراحات أثناء تصفح التطبيق والتنقل بين التبويبات طوال اليوم، ولا تتغير إلا عندما يطلب المستخدم تحديثها يدوياً أو يطبخ وجبة.
  > **الوصف التقني:**
  > 1. في `cooldown_engine.dart`، تعديل آلية اختيار المرشحين بعد الفلترة لتعتمد على خلط عشوائي حقيقي بالـ `shuffleSeed` أو تدوير القائمة المؤهلة (`candidates`) قبل تطبيق قواعد تنوع البروتين.
  > 2. التأكد من ربط `refreshSeedProvider` في `home_screen.dart` بإعادة حساب فورية تُنتج وجبات مختلفة تماماً.
  > **الحالة:** ✅ منفذة. تم نقل العشوائية من درجة الوجبة إلى مرحلة الاختيار في `cooldown_engine.dart`: حُذف حدّ الـ `jitter` (كان `[0, 3.96]` وأقل من فارق الترتيب الحقيقي)، وأصبح الاختيار يعتمد على سحب عشوائي مُبذّر بـ `Random(daysSinceEpoch + shuffleSeed * 7919)` داخل نطاق درجات بعرض `5.0` (`_interchangeableBand`)، فالتحديث اليدوي يغيّر الوجبات فعلياً مع بقاء الثبات طوال اليوم. التغطية: `test/unit/recommendation_variety_test.dart`.

- [x] **منع إعادة ترتيب أو تبديل كروت الوجبات عند الضغط على زر الإعجاب (Prevent Recommendation Re-ranking on Favorite Toggle)**:
  - المشكلة: عند الضغط على زر الإعجاب (أيقونة القلب/Love) على أي كارت وجبة في الصفحة الرئيسية، يتم إعادة ترتيب وتبديل الوجبات الثلاث المقترحة فوراً بشكل عشوائي ومزعج (الكارت يقفز للأعلى أو ينزل للأسفل أو يتم استبداله بوجبة أخرى تماماً). ومع تكرار الضغط، تتغير أماكن الكروت باستمرار مما يجعل المستخدم ينقر بالخطأ على كارت آخر.
  - السبب التقني:
    1. عند تبديل حالة المفضلة في قاعدة البيانات عبر `mealsDao.toggleFavorite`، يُصدر `allMealsProvider` قائمة الوجبات المحدثة.
    2. مزود الاقتراحات `todayRecommendationsProvider` يراقب `allMealsProvider` بشكل مباشر، مما يؤدي لإعادة تشغيل خوارزمية الحساب `engine.compute` بالكامل عند كل نقرة.
    3. بما أن الخوارزمية تمنح الوجبة المفضلة 5 نقاط إضافية (`sFavorite = 5.0`)، فإن تغيير حالة المفضلة يُغير درجات الوجبة وترتيبها الفوري، مما يجعلها تقفز فوق الوجبات الأخرى وتتبدل أماكن الكروت على الشاشة.
  - المطلوب:
    1. ثبات واستقرار كروت الوجبات: عند الضغط على زر القلب/الإعجاب، يجب فقط تحديث حالة القلب (ممتلئ/فارغ) وحفظها في قاعدة البيانات، مع بقاء الوجبات الثلاث في أماكنها وترتيبها الحالي تماماً دون أي قفز أو تبديل.
    2. عدم إعادة تشغيل خوارزمية الترتيب الكاملة عند مجرد تعديل المفضلة؛ بل تحديث خاصية `isFavorite` للوجبة المعروضة فقط داخل القائمة الحالية دون تغيير ترتيب أو هويات الوجبات المقترحة.
  > **الوصف التقني:**
  > 1. في `todayRecommendationsProvider` أو متحكم التوصيات، فصل حالة اختيار الوجبات الثلاث المقترحة (Selection State) عن التحديثات اللحظية لخصائص الوجبات، بحيث يُحافظ على معرّفات الوجبات الثلاث الحالية (`mealIds`) وتحديث كائن الـ `Meal` نفسه فقط عند ورود تحديث من قاعدة البيانات.
  > 2. حصر إعادة تشغيل `engine.compute` فقط عند طلب المستخدم الصريح للتحديث (Pull-to-refresh) أو عند تسجيل طبخ وجبة اليوم.
  > **الحالة:** ✅ منفذة. تحوّل `todayRecommendationsProvider` من `Provider` إلى `NotifierProvider` (`TodayRecommendationsNotifier`) يتذكر المعرّفات المختارة عبر `_eligibilityKey` (اليوم + مفتاح التحديث + عدد السجل + مجموعة معرّفات الخزانة + إعدادات التهدئة). تغيير `isFavorite` لا يغيّر المفتاح، فتُعاد بناء نفس الوجبات بنفس الترتيب من صفوف قاعدة البيانات الحديثة (مع بياناتها المحدّثة)، ولا يُعاد تشغيل `engine.compute` إلا عند التحديث اليدوي أو الطبخ أو الإضافة/الحذف.
  > **تحديث جرد 25 سبتمبر (البند كان 90 %):** المفتاح كان يعتمد على **عدد** صفوف
  > السجل فقط، فحركة طبخ وجبة ليوم آخر — أو حذف صف وإضافة صف في إطار واحد — تترك
  > تثبيت اليوم غالياً دون أن تُعاد الحساب. استُبدل العدد بتوقيع فعلي يعيد ما يقرأه
  > المحرك: أحدث يوم طبخ لكل معرّف أكلة. وأُضيفت إلى توقيع الأكلات الحقول الثلاثة
  > التي يقررها المحرك فعلاً: `proteinType` (يحدد نافذة التهدئة ومحور التنوع الأول)
  > و`carbsType` (محور التنوع الثاني) و`isFridaySpecial` (+15 يوم الجمعة و−5 في بقية
  > الأيام، أي قفزات أوسع من نطاق التبادل `5.0`). **بقي المتعمَّد كما هو:**
  > `isFavorite` و`isBudgetFriendly` والاسم والاسم المختصر والصورة ووقت التحضير
  > والتصنيف و`updatedAt` خارج المفتاح عمداً — لأن جوهر هذا البند أن نقرة القلب لا
  > تحرك كارتاً. التغطية صارت 15 اختباراً في
  > `test/unit/recommendation_variety_test.dart` (من محفّزات إعادة الترتيب الخمسة لكل
  > اختبار، مع اختبارات عكسية تمنع الحساسية الزائدة).
  > **تصحيح لازم:** نص البند الثاني في هذا الملف وفي `app_v2/ISSUES.md` كان يوجّه
  > إلى دالة باسم `_rankAndSelectDiversity` لا وجود لها في `cooldown_engine.dart`؛
  > الأسماء الحقيقية `_rankCandidates` (النطاق + السحب المبذّر) و`_selectWithNovelty`
  > و`_selectDiverse`.

---

## 🆕 مشاكل مكتشفة أثناء المراجعة (21 سبتمبر) — خارج القائمة الأصلية

- [x] **كروت اقتراحات الصفحة الرئيسية لا تستجيب للنقر (MealCard.onTap was never passed)**:
  - المشكلة: كانت `MealCard` تعرّف بارامتر `onTap` اختياري يغلّف الـ `InkWell` الداخلي، لكن `home_screen.dart` لم يمرّره أبداً — فنقر المستخدم على أي كارت اقتراح لا يفتح شيئاً، رغم أن `MealDetailsSheet` جاهزة ومربوطة بكل الشاشات الأخرى (السجل، الاستكشاف، الخزنة).
  - ما تم: تمرير `onTap` لفتح `MealDetailsSheet.show` بسياق `vault`، مع ربط `onEdit` بـ `QuickAddSheet.show(mealToEdit:)` و `onDelete` بـ `DeleteMealDialog.show` — أي نفس ما يفعله `meal_vault_card.dart`. تمرير الاستدعاءين معاً ضروري لأن الودجت تعرض زري التعديل والحذف دائماً وتتجاهل النقر بصمت إذا لم يُمرَّر الـ callback، فكانا سيظهران للمستخدم كزرّين لا يستجيبان.
  > **الحالة:** ✅ منفذة.

- [x] **ثغرتا مغادرة تتجاوزان `PopScope` في `QuickAddSheet` (باقٍ من بند التعديلات غير المحفوظة)**:
  1. **السحب للأسفل**: `showModalBottomSheet` يُبقي `enableDrag: true` افتراضياً، وحركة السحب تنتهي بـ `Navigator.pop` مباشرة دون استشارة `popDisposition`، فتُفقد التعديلات بلا أي تأكيد. الحل المقترح: `enableDrag: false`، أو تمرير `onClosing` يوجّه الإغلاق إلى `_requestClose`.
  2. **التبديل إلى تبويبة أخرى**: السيت يُدفع على الـ navigator الخاص بفرع الخزنة (`useRootNavigator: false`)، لذا تبقى شريط التبويبات السفلي خارج الحاجر وقابل للنقر، فلا يظهر التأكيد ويبقى السيت مخفياً ليعود لاحقاً. (`ProfileEditDialog` يستخدم `showDialog` على navigator الجذر، فلا تتطبق عليه هذه الثغرة.)
  3. **`canPop` تصبح قيمته قديمة عند الكتابة النصية**: لا يوجد `onChanged` أو `addListener` على حقلَي الاسم والبريد، فلا تُعاد بناء `profile_edit_dialog.dart` عند الكتابة وحدها، وتبقى `canPop: true` فيخرج زر الرجوع بصمت. (`quick_add_sheet.dart` محمي بالصدفة فقط لأنه يقرأ `MediaQuery.viewInsets`.)
  > **الحالة:** ✅ منفذة بالكامل — كانت 33 % ومُعلَّمة ❌ بلا اختبار واحد يغطي
  > `hasUnsavedChanges`.
  > **تحديث جرد 25 سبتمبر:** ✅ أُغلقت الثلاث (كانت 33 %).
  > 1. **السحب للأسفل** — أُغلق بـ `enableDrag: false` على `QuickAddSheet.show`؛ حركة
  >    السحب كانت تنهي نفسها بـ `Navigator.pop` مباشر لا يستطيع `PopScope` الاعتراض
  >    عليه، فالورقة طويلة ولها زر إغلاق وزر إلغاء والنقر خارجها.
  > 2. **التبديل إلى تبويبة أخرى** — كانت أخطر مما وُصفت: السيت كان يُعرض على مراقب
  >    فرع الخزنة، فالشريط السفلي يبقى خارج الحاجز وقابلاً للنقر فعلاً (قياساً على
  >    400×900: الوجهات الأربع كلها `hitTestable`)، والنقر على تبويبة كان **يدمّر**
  >    مسار السيت ولا يخفيه فقط — أي تضيع المسودة المكتوبة. أُصلح بعرض السيت على
  >    مراقب الجذر (`useRootNavigator: true`) فصار الحاجز يغطي الشريط كله؛ وثبت أن
  >    الشكل لم يتغير لأن سقف الـ 640dp يأتي من ثيم Material لا من المراقب المضيف
  >    (قياس 1280×900 قبل وبعد: العرض 640 والمركز 640، والفرق الوحيد أن الحافة السفلى
  >    صارت ملاصقة للشاشة بدل أن تنتهي فوق الشريط)، وأن `ref` والترجمة و`Directionality`
  >    ما زالت تُقرأ لأن `ProviderScope` و`Localizations` فوق مراقب المسار.
  > 3. **`canPop` القديمة عند الكتابة** — أُغلقت بـ `addListener` على الحقول (تفصيلها
  >    في بند التعديلات غير المحفوظة أعلى).
  >    التغطية الآن: `test/widget/unsaved_changes_guard_test.dart` (10) و
  >    `test/widget/quick_add_sheet_nav_barrier_test.dart` (3، من بينها قياس tablet
  >    وقياس أن نقرة الحاجز تُفتح الحوار ثم «متابعة التعديل» تُبقي النص).

- [x] **سلوك مُضلّل في إشعار زر الرجوع العام**: عند الرجوع من تبويبة ثانوية ينتقل التطبيق للرئيسية ويُظهر إشعار «اضغط مرة أخرى للخروج»، مع أن الضغطة التالية تُخرج من التطبيق فعلاً؛ ولأن الفرع نفسه يضبط `_lastBackPressTime` فإن ضغطة ثانية سريعة تُغلق التطبيق مباشرة بدل تحذير المستخدم مرة واحدة وهو على الرئيسية.
  > **الحالة:** ✅ العطل مُصلح — بقي قرار واحد في الصياغة. فرع التبويبة الثانوية كان
  > يُظهر
  > «اضغط مرة أخرى للخروج» ويُسلّح المؤقّت في نفس اللحظة، فالدخول التالي يخرج من
  > التطبيق بلا تحذير حقيقي. الآن التسليح لا يحدث إلا من ضغطة تقع وهو على الرئيسية،
  > وتبديل التبويبة يُلغي أي تحذير عالق. التفصيل والشواهد في بند «سلوك زر الرجوع
  > الموحد» أعلى، والتغطية `test/widget/global_back_button_test.dart`.
  > **الباقى قرار منتج محض:** هل يظهر الإشعار عند الانتقال من تبويبة إلى الرئيسية أم
  > لا؟ النصّان في هذا الملف متناقضان (سطر 57 يقول يظهر، وهذا البند يقول لا)، وطبّقنا
  > الأحدث.

---

## 📌 ما زال مفتوحاً بعد جرد 25 سبتمبر — لا يقدر عليه وكيل

هذه بنود خارجة عن الكود: إما فعل على إنتاج مشترك أو قرار منتج، ومسجّلة هنا حتى لا تُنسى.

- [x] **حذف وثيقة التشخيص من `staging_meals`** — `TEST_DIAG_DELETE_ME` — ✅ تم حذف الوثيقة بنجاح من قاعدة بيانات Firestore المباشرة على مشروع `daily-meal000` عبر Firebase CLI.
- [x] **الملاحظات والاسم المختصر لا يُحرران من شاشة الأكلة** — ✅ تم حلها من جذورها بالكامل: إضافة حقلين `shortName` و `notes` في `QuickAddSheet`، وربطهما بـ `VaultController.addMeal` و `VaultController.updateMeal` وقاعدة بيانات SQLite، مع ربطهما بحارس التعديلات غير المحفوظة `hasUnsavedChanges`، وتوفير كافة مفاتيح الترجمة في `app_strings.dart` بالعربية والإنجليزية بدون أي نص ثابت، وإضافة اختبارات شاملة في `test/widget/quick_add_sheet_notes_test.dart` (3/3 ناجحة).
- [ ] **رفع Cloudinary بلا توقيع** — الـ preset غير موقّع والحد اليومي محلي في
  الهاتف، فليس منعاً حقيقياً لإساءة الاستخدام. يحتاج قيود على الـ preset أو دالة
  Function توقّع الرفع.
- [x] **تحذير واحد متروك عمداً** — `app_config_sync_service.dart:114` — ✅ تم حله من جذوره بنقل فحص الاتصال `ReachabilityService.isInternetReachable` إلى ما قبل قفل `_isSyncing = true` وخارج كتلة `try/finally`، مما أزال التحذير تماماً وصار كود التحليل يحقق صفر تحذيرات وصفر أخطاء دون التأثير على آليات المزامنة.
- [ ] **مرور يدوي على الجهاز** (لا تغطيه الاختبارات مهما كثرت): نقر إعجاب على كارت
  اقتراح · خروج بنصف مكتوب في ورقة إضافة · اقتراح والشبكة انقطعت أثناء الرفع · فتح
  صورة اقتراح على جهاز بتكبير نص 1.5×.

---

## 🛡️ خطة الحماية والصلابة المعمارية الكبرى (Master Hardening & Bug-Fixing Roadmap — Sep 25)

تم جرد وتحليل 4 تقارير تشخيصية و10 خطط تنفيذ، وتنقية الهلاوس، وتقسيم العمل إلى 10 مهام دقيقة نُفذت جميعها بنجاح عبر كودر وبإشراف وتحقق مستقل:

- [x] **المهمة 1: إصلاح خطر حذف الصور في `OrphanImageSweeper`**:
  - المشكلة: عند فشل قاعدة البيانات كانت الدالة تعيد قائمة فارغة، فيظن المعالج أن كل الصور أيتام ويحذفها من الذاكرة.
  - الحل: إرجاع `null` لإلغاء الكنس فوراً، مع إضافة ضابطي أمان (عدم حذف أكثر من 50% أو أكثر من 20 صورة في المرة الواحدة).
  - الحالة: ✅ منفذة بالكامل مع 8 اختبارات وحدة (`test/unit/orphan_image_sweeper_test.dart`).

- [x] **المهمة 2: توحيد إعدادات الكولداون ومنع تصفير خيارات المستخدم**:
  - المشكلة: دالة `_selfHealSchema()` كانت تنفذ استعلام `UPDATE` عند كل إقلاع يعيد ضبط أيام الكولداون إلى القيم الافتراضية، ماسحة تفضيلات المستخدم، مع تضارب القيم الافتراضية بين الملفات.
  - الحل: حذف استعلامات التصفير من الإقلاع وتوحيد القيم الافتراضية إلى `2, 2, 4, 0`.
  - الحالة: ✅ منفذة بالكامل مع 6 اختبارات (`test/unit/migration_self_healing_test.dart`).

- [x] **المهمة 9: حماية الخصوصية ومزامنة العناصر الغذائية في لوحة الإدارة (`Admin Auth Screen`)**:
  - المشكلة: اعتماد وجبات الاقتراح كان ينقل معرّف المستخدم المجهول `proposedBy` إلى وثائق `vault_meals` العامة، مع نقص خيارات `dairy` و `potato` و `grains` في نافذة الإضافة.
  - الحل: حذف حقل `proposedBy` قبل الحفظ في الخزنة العامة، ومطابقة كافة خيارات البروتين والكارب مع التطبيق، وتأمين `firestore.rules`.
  - الحالة: ✅ منفذة بالكامل (`flutter analyze` صفر أخطاء).

- [x] **المهمة 3: المعاملة الذرية لتنزيل الأكلات كجديدة ومنع إشعارات النجاح الزائفة**:
  - المشكلة: عملية التنزيل كجديدة `downloadAsNew` كانت تجري خارج معاملة ذرية وتُظهر توست النجاح قبل اكتمال الشبكة أو قاعدة البيانات.
  - الحل: توطين الصور أولاً، ثم دمج فك الارتباط والإدراج في معاملة ذرية `_db.transaction` واحدة، وانتظار النتيجة في الواجهة لعرض إشعارات حقيقية.
  - الحالة: ✅ منفذة بالكامل مع اختبارات (`test/unit/discovery_download_atomic_test.dart`).

- [x] **المهمة 4: مزامنة أكلات البداية عبر `cloudId` وحماية الحذف العشوائي**:
  - المشكلة: الاعتماد على المطابقة بالاسم كان يفشل عند تعديل الأسماء، وحذف الأكلات غير الموجودة كان يمسح سجل الطبخ والمفضلات للمستخدمين.
  - الحل: المطابقة عبر `cloudId`، وإلغاء صفة البداية بنعومة `isStarterMeal: false` دون حذف الأكلة أو سجلها، وحماية الاستجابة الفارغة.
  - الحالة: ✅ منفذة بالكامل مع اختبارات (`test/unit/app_config_sync_service_test.dart`).

- [x] **المهمة 5: إعادة جدولة الإشعارات عند تغيير اللغة وإلغاء المنبه عند تصفير الإعدادات**:
  - المشكلة: تصفير الإعدادات لم يكن يلغي منبه النظام، وتغيير اللغة كان يترك الإشعارات القديمة، وفشل التفعيل لم يكن يتراجع عن الحالة.
  - الحل: ربط `resetToDefaults` بإلغاء المنبه، و`updateLanguage` بإعادة الجدولة باللغة الجديدة، والتراجع التلقائي عند الخطأ.
  - الحالة: ✅ منفذة بالكامل مع 8 اختبارات (`test/unit/settings_notification_sync_test.dart`).

- [x] **المهمة 6: تحصين تصاريح أندرويد والالتزام بسياسات Google Play**:
  - المشكلة: نقص تصريح `INTERNET`، ووجود تصريح `USE_EXACT_ALARM` المحظور على تطبيقات التذكير في متجر جوجل، واستخدام اسم افتراضي.
  - الحل: إضافة تصريح الإنترنت، إزالة التصريح المحظور، تحويل الجدولة إلى `inexactAllowWhileIdle`، وتغيير اسم التطبيق رسمياً إلى «أكلة النهاردة».
  - الحالة: ✅ منفذة بالكامل.

- [x] **المهمة 7: تأمين تنزيل الصور عبر `MealImageLocalizer`**:
  - المشكلة: السماح بروابط غير مشفرة، وغياب التحقق من النطاقات الموثوقة، وخطر نفاد الذاكرة من الملفات الكبيرة.
  - الحل: فرض HTTPS، وحظر النطاقات غير الموثوقة، وتحديد سقف 5 ميغابايت مع التدفق المباشر وفحص نوع الملف `image/*`، والتحميل في ملف مؤقت `.tmp` قبل الاستبدال الذري.
  - الحالة: ✅ منفذة بالكامل مع 12 اختباراً (`test/unit/meal_image_localizer_test.dart`).

- [x] **المهمة 8: تعريب وتوطين سجل الوجبات وإلغاء النصوص الثابتة**:
  - المشكلة: تسجيل نصوص عربية صلبة (`خارج البيت` و`تفويت الوجبة`) في قاعدة بيانات SQLite مما كان يمنع تعريبها عند التحويل للإنجليزية.
  - الحل: حفظ مفاتيح محايدة لغوياً (`takeout`, `skipped`)، وتوفير محلل ديناميكي في `AppStrings.historyEntryDisplayName` يدعم المفاتيح الجديدة والصفوف القديمة بسلاسة.
  - الحالة: ✅ منفذة بالكامل مع 11 اختباراً (`test/unit/history_stats_test.dart`).

- [x] **المهمة 10: حماية إقلاع Firebase في وضع عدم الاتصال وترقيم صفحات الاستكشاف**:
  - المشكلة: تعطل التطبيق أو ظهور أخطاء استثناءات متكررة `[core/no-app]` عند فتح التطبيق بدون إنترنت أو في الاختبارات، وجلب كل أكلات الخزنة دفعة واحدة.
  - الحل: توفير `firebaseAvailableProvider`، وحماية تدفق الإشعارات عند غياب Firebase، ودعم الترقيم التدريجي `limit(50)` و `startAfterDocument` في `DiscoveryRepository`.
  - الحالة: ✅ منفذة بالكامل مع 7 اختبارات (`test/unit/discovery_offline_and_pagination_test.dart`).

**حصيلة بوابات الجودة (25 سبتمبر):**
- `flutter analyze --no-pub`: **0 أخطاء، 0 تحذيرات**.
- `flutter test --no-pub`: **281/281 اختباراً ناجحاً (100%)**.

---

## 🚀 خطة التدقيق والتحسين الشامل للنماذج المتعددة (Multi-Model Audit & Refinement — 26 سبتمبر)

تم فحص وتحليل كافة مخرجات وتقارير النماذج الذكية (Claude Opus, Gemini 3.8 Flash, GPT-5.6 Sol v1 & v2) وتصفية وتوحيد كافة الملاحظات في 10 مهام دقيقة تم تنفيذها بالكامل وإخضاعها للاختبارات الحية:

- [x] **المهمة 1: حماية ترقية المخطط وحفظ أداء الإقلاع وتوحيد قيم الكولداون**:
  - المشكلة: تشغيل دالة `_selfHealSchema()` الثقيلة مع كل إقلاع للتطبيق واستهلاك الموارد، واختلاف القيم الافتراضية للكولداون بين المحرك والواجهة.
  - الحل: تقييد تشغيل الترقيم بعلم `SharedPreferences` بحيث لا يُنفذ إلا لمرة واحدة عند ترقية المخطط فقط، مع استثناء بيئة الاختبار `FLUTTER_TEST` لمنع التعليق، وتوحيد قيم الكولداون مع `SystemDefaults` (لحم: 2، فراخ: 2، سمك: 4، خضار: 0).
  - الحالة: ✅ منفذة بالكامل (`4b62128`).

- [x] **المهمة 2: تنظيف الحزم غير المستخدمة ودمج الكاش الشبكي للصور**:
  - المشكلة: وجود حزمة `firebase_remote_config` دون أي استخدام فعلي داخل التطبيق، والحاجة لحزمة تخزين كاش للصور.
  - الحل: حذف `firebase_remote_config` من `pubspec.yaml` وإضافة `cached_network_image: ^3.4.1`.
  - الحالة: ✅ منفذة بالكامل (`ee282a5`).

- [x] **المهمة 3: إزالة الأكواد الميتة من خدمة الأفاتار `AvatarService`**:
  - المشكلة: بقاء دوال قديمة لتحميل الصور عبر الإنترنت (`_downloadAvatars`, `_downloadFromUrl`) رغم أن الصور مضمنة محلياً في التطبيق.
  - الحل: حذف كافة الدوال الميتة وتنظيف الاستيرادات وتبسيط دالة نسخ الصور المضمنة إلى مجلد المستندات.
  - الحالة: ✅ منفذة بالكامل (`47ead06`).

- [x] **المهمة 4: ترقية منبه الإشعارات إلى المنبه الدقيق وتعريب قنوات الإشعارات**:
  - المشكلة: استخدام `inexactAllowWhileIdle` قد يؤدي لتأخير إشعارات الوجبة اليومية، مع وجود قنوات إشعارات أندرويد بنصوص إنجليزية ثابتة غير معربة.
  - الحل: استخدام `exactAllowWhileIdle` مع حماية التراجع التلقائي إلى `inexactAllowWhileIdle` عند رفض الصلاحية، وتعريب أسماء ووصف القنوات بالكامل عبر `AppStrings`.
  - الحالة: ✅ منفذة بالكامل (`de819d9`).

- [x] **المهمة 5: تحصين مسارات الروابط العميقة للإشعارات وإصلاح إقلاع التطبيق البارد**:
  - المشكلة: إمكانية حقن مسارات غير صالحة من حمولة الإشعارات، والاعتماد على مؤقت اعتباطي هش (300 مللي ثانية) للتوجيه عند فتح التطبيق من إشعار وهو مغلق تماماً.
  - الحل: إنشاء كلاس `NotificationRoute` لتعقيم المسارات عبر قائمة بيضاء موثوقة، واستبدال المؤقت بـ `WidgetsBinding.instance.addPostFrameCallback`.
  - الحالة: ✅ منفذة بالكامل مع اختبارات وحدة (`6d46252`).

- [x] **المهمة 6: تحصين قواعد أمان Firestore وقيد حذف أكلات الخزنة**:
  - المشكلة: عدم إلزامية حقل `proposedBy` في أكلات الاقتراح، وتوقف عمليات حذف الأكلات في الخزنة لفشل فحص `request.resource.data` أثناء الحذف، وحاجة وثائق الإعدادات لفصل صريح.
  - الحل: إلزامية `proposedBy` في `isValidStagingMeal()`، وفصل قواعد الحذف عن الإضافة والتعديل في `vault_meals`، وفصل وثائق `admin_config` وتطبيق القواعد في مجلدي التطبيق والموقع.
  - الحالة: ✅ منفذة بالكامل (`d7d5ca0`).

- [x] **المهمة 7: التدقيق الأمني للوحة الإدارة `Admin Auth Screen`**:
  - النتيجة: تم التحقق بالكامل من خلو مشروع الويب من أي تخزين محلي غير آمن لكلمات المرور أو الهاشات، واعتماد لوحة التحكم كلياً على `FirebaseAuth` الموثوق ضد مجموعة `/admins` المعتمدة في Firestore.
  - الحالة: ✅ تم التحقق بنجاح.

- [x] **المهمة 8: تفعيل الكاش في شاشة الاستكشاف وإصلاح رسائل النجاح الكاذبة**:
  - المشكلة: استخدام `Image.network` الخام دون تخزين مؤقت أو مؤشرات تحميل، وعرض رسائل نجاح التنزيل والتحديث حتى في حالات الفشل الشبكي.
  - الحل: ترقية ودجت `MealImage` لاستخدام `CachedNetworkImage` مع تأثير الشيمر وأيقونات الخطأ، وانتظار حالة العملية الحقيقية قبل إظهار إشعارات النجاح أو الفشل في شاشات الاستكشاف والتفاصيل والمزامنة.
  - الحالة: ✅ منفذة بالكامل (`72f6281`).

- [x] **المهمة 9: مطابقة تصنيفات الأكلات السحابية وإصلاح تاريخ «أمس» عند بداية الشهر**:
  - المشكلة: عدم مطابقة تصنيفي `soup_stew` و `vegetarian` بين التطبيق ولوحة الإدارة، وظهور خطأ في حساب يوم «أمس» في السجل عند أول يوم في الشهر الميلادي (`now.day - 1`).
  - الحل: تحديث خريطة `MealCloudVocabulary` لمطابقة كافة التصنيفات، واستخدام دالة `app_date_utils.formatHistoryDate` الموحدة لحساب التاريخ وضبط حدود الأشهر والسنوات بدقة.
  - الحالة: ✅ منفذة بالكامل مع اختبارات (`36094da`).

- [x] **المهمة 10: ضبط الحجم التكيفي لكاش الصور وفقاً لكثافة شاشة الجهاز**:
  - المشكلة: حجز حجم كاش ثابت كبير جداً (200 ميغابايت / 1200 صورة) مما يرهق ذاكرة الأجهزة الاقتصادية القديمة.
  - الحل: إنشاء دالة `configureImageCache()` تضبط حجم الكاش ديناميكياً بناءً على نسبة بكسلات الشاشة (`devicePixelRatio`): 96 ميغابايت / 500 صورة للأجهزة العادية، و 160 ميغابايت / 900 صورة للأجهزة عالية الكثافة.
  - الحالة: ✅ منفذة بالكامل مع اختبارات (`b9d9a4f`).

**حصيلة بوابات الجودة النهائية للجولة الأولى:**
- `flutter analyze --no-pub`: **0 أخطاء، 0 تحذيرات (31 ملاحظة استشارية)**.
- `flutter test --no-pub`: **284/284 اختباراً ناجحاً بنسبة 100%**.

---

## 🛡️ الجولة الثانية من مراجعة الموديلز وتحصين النظام (Multi-Model Audit Hardening — Round 2 — 26 سبتمبر)

مراجعة شاملة لتقارير الموديلز الثلاثة الجديدة (Kimi K3، Claude Opus 5 Medium، 01a0da88) وتنفيذ كافة الملاحظات الحقيقية عبر كودر:

- [x] **المهمة 1 & 4: حماية إعادة ضبط الإعدادات وموجه المسارات وخمول التبريد**:
  - المشكلة: دالة `resetToDefaults()` كانت تكتب `isFirstRun: true` وتعيد ضبط اللغة، فتقذف بالمستخدم الحالي إلى شاشة الترحيب وتلغي اختياره للغة، كما أن قراءة التاريخ الفاسد في محرك التباعد الزمني كانت تُرجع `DateTime.now()` مما يحجب الأكلة لمدة 14 يوماً كاملة، ومسار `/meal/:id` يمرر `-1` عند وجود معرف تالف.
  - الحل: إزالة `isFirstRun` و `language` من إعادة الضبط وإبقاء إعدادات المستخدم، وحصر وقت التنبيه في الحدود الصحيحة (0-23، 0-59) على مستوى الـ DAO، واستبدال التاريخ الفاسد بـ `DateTime.fromMillisecondsSinceEpoch(0)` (تاريخ محايد لا يحجب الوجبة)، وإضافة حارس توجيه لمسار `/meal/:id` يعيد إلى الشاشة الرئيسية عند المعرفات غير الصالحة.
  - الحالة: ✅ منفذة بالكامل (`e966495`).

- [x] **المهمة 2: تفعيل حارس `CloudPolicy` لاحترام سياسة «السحابة على Wi-Fi فقط»**:
  - المشكلة: مفتاح «السحابة على واي فاي فقط» كان يُحترم في شاشات الاستكشاف والاقتراح فقط، بينما كانت العمليات الخلفية (مزامنة الإعدادات، تنزيل صور الخزنة، فحص إشعارات الأدمن) تستهلك باقة الموبايل عند كل إقلاع.
  - الحل: إنشاء كلاس `CloudPolicy.isCloudAllowedNow()` وحقنه كبوابة مبكرة في الخدمات الثلاث: `AppConfigSyncService` و `MealImageLocalizer` و `NotificationSyncService`.
  - الحالة: ✅ منفذة بالكامل (`3056f0c`).

- [x] **المهمة 3: تفعيل Firebase App Check وتأمين روابط الصور ومخطط إشعارات الأدمن**:
  - المشكلة: غياب App Check تماماً مما يسمح بالوصول لقاعدة البيانات من أي عميل غير رسمي، وقبول أي رابط في `imageUrl` باقتراحات الوجبات، وغياب التحقق من حقول وأحجام إشعارات الأدمن.
  - الحل: تفعيل حزمة `firebase_app_check` مع `AndroidDebugProvider` و `AndroidPlayIntegrityProvider`، وفرض `https://` على روابط الصور في قواعد Firestore، وإضافة دالة `isValidAdminNotification()` للتحقق الصارم من الحقول والأحجام وقفل وثيقة `admin_config/security` ضد قراءات الأدمن العاديين، ومزامنة القواعد مع موقع لوحة الإدارة.
  - الحالة: ✅ منفذة بالكامل (`7e88afd`).

- [x] **المهمة 5: تحصين مزامنة الوجبات الافتراضية وحماية الصور وتطبيع الأسماء**:
  - المشكلة: كتابة `Value(null)` للصورة في المزامنة كانت تمسح الصورة المحلية السليمة، ومطابقة الأسماء الحرفية في Pass 2 كانت تُنتج وجبات مكررة بسبب اختلافات إملائية عربية طفيفة (ة/ه، ى/ي)، والمزامنة الخلفية كانت تحدّث `updatedAt` فتكسر عداد منع تكرار الاقتراح.
  - الحل: استخدام `const Value.absent()` عند غياب الصورة لحفظ الصورة المحلية، واستخدام `normalizeArabic` في مفاتيح المطابقة، وإضافة معامل `touchUpdatedAt: false` لمنع تحديث وقت التعديل أثناء المزامنة التلقائية.
  - الحالة: ✅ منفذة بالكامل (`3b8814f`).

- [x] **المهمة 6: تنظيف صلاحيات أندرويد وإلغاء النسخ الاحتياطي التلقائي وتوطين اسم التطبيق**:
  - المشكلة: وجود صلاحيات زائدة في `AndroidManifest.xml` (`SCHEDULE_EXACT_ALARM`, `CAMERA`, `READ_MEDIA_IMAGES`) مما قد يسبب رفض التطبيق في متجر Google Play، مع نسخ بيانات SQLite محلياً إلى Google Drive بدون تحكم، وثبات اسم التطبيق بالعربية فقط في أندرويد.
  - الحل: حذف الصلاحيات الزائدة، وتعيين `android:allowBackup="false"`، وتوطين اسم التطبيق عبر `@string/app_name` في `values/strings.xml` و `values-ar/strings.xml`، وفصل التقاط الصور عن فحص صلاحية الكاميرا المباشرة في أندرويد للاعتماد على Intent النظامي الآمن.
  - الحالة: ✅ منفذة بالكامل (`d63c3d3`).

- [x] **المهمة 7 & 8: موثوقية التنبيهات اليومية وإعادة التسليح عند الإقلاع وتحديد سقف إشعارات الأدمن**:
  - المشكلة: الاعتماد على متغير ذاكرة `_exactAlarmsAllowed` كان يضيع بعد إعادة تشغيل التطبيق، وغياب إعادة جدولة التنبيه عند بدء التشغيل، وعرض إشعارات الأدمن حتى عند إيقاف التنبيهات من الإعدادات وبدون سقف للعدد.
  - الحل: فحص قدرة النظام الحية على المنبه الدقيق `canScheduleExactNotifications()` قبل كل جدولة مع التراجع التلقائي، وإعادة تسليح التنبيه تلقائياً عند إقلاع التطبيق إذا كان مفعّلاً، وربط إشعارات الأدمن بـ `notificationsEnabled` مع سقف 3 إشعارات كحد أقصى لكل إقلاع وتقليم النصوص الطويلة حمايةً من أي حمولات زائدة.
  - الحالة: ✅ منفذة بالكامل (`372e0f0`).

- [x] **المهمة 9 & 10: ضبط استعلام البواقي وإزالة عداد الشعبية المصطنع واستبعاد كاش Firebase**:
  - المشكلة: استعلام `getLatestCookedMeal()` كان يجلب أي سجل أخير بما فيها التيك أواي والتخطي فيُسجل بواقي عليها خطأً، ووجود عداد مفبرك `1.2 + (index * 0.3)` في شاشة الاستكشاف، وتتبع ملف كاش `.firebase/` في Git.
  - الحل: قصر استعلام آخر وجبة مطبوخة على `entryType == MealEntryType.cooked`، وحذف العداد المصطنع من ودجت الاستكشاف، وإضافة `.firebase/` إلى `.gitignore` وحذفها من فهرس Git.
  - الحالة: ✅ منفذة بالكامل (`a5730b4`).

**حصيلة بوابات الجودة الإجمالية:**
- `flutter analyze --no-pub`: **0 أخطاء، 0 تحذيرات (31 ملاحظة استشارية)**.
- `flutter test --no-pub`: **284/284 اختباراً ناجحاً بنسبة 100%**.


