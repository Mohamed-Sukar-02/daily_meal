# worker_backlog — changes
- CREATED ISSUES.md (repo root):
  - Closed [x]: like-reorder fix, favourite-jitter fix, vault header clip, PopScope/unsaved
    changes (per plan note — no code change), Cloud Staging Export, meal views/navigation.
  - Rejected [~]: flutter_image_compress (native picker compression + 500KB gate instead);
    stale "PopScope not implemented" backlog entry.
  - Open [ ]: rules deployment (firebase deploy --only firestore:rules,storage), Cairo font
    bundling (perf worker escalation), admin triage surface for staging_meals.
  - Verification checklist mirrors the plan's Automated/Manual sections with sandbox caveats.
