<task>
Fix the data-loss bug in `lib/core/services/orphan_image_sweeper.dart` in `e:\Mohamed\Personal_Project\daily-meal\app_v2`.

CURRENT BEHAVIOR:
In `_getDbPhotoPaths(AppDatabase db)`:
```dart
try {
  final meals = await db.mealsDao.getAllMeals();
  ...
  return paths;
} catch (e) {
  debugPrint('Failed to get DB photo paths: $e');
  return <String>{};
}
```
If `getAllMeals()` fails (e.g., SQLite lock, corruption, migration, or any unexpected DB exception), `_getDbPhotoPaths` catches the exception and returns an empty Set (`<String>{}`).
Then `sweepAtStartup` passes this empty set to `_sweepInIsolate(docsPath, dbPaths)`.
In `_sweepInIsolate`:
Because `dbPaths` is empty, `referencedBasenames` and `referencedFullPaths` are empty.
The sweeper assumes that ALL photos in `meal_images` older than 1 hour are orphans and permanently deletes all user meal photos from disk!

WHAT MUST CHANGE:
1. In `lib/core/services/orphan_image_sweeper.dart`:
   - Change `_getDbPhotoPaths(AppDatabase db)` to return `Future<Set<String>?>` (or a Result type). If an exception occurs when querying the database, log the error and return `null`.
   - In `sweepAtStartup(AppDatabase db)`:
     If `dbPaths == null`, immediately abort the sweep and do NOT invoke `_sweepInIsolate`.
   - In `_sweepInIsolate`:
     Add a blast-radius safeguard: if `dbPaths.isEmpty` and the directory contains files, do not delete everything silently unless explicitly safe; if a large batch would be wiped, log an abort warning.
2. In `test/unit/orphan_image_sweeper_test.dart`:
   - Add unit tests validating:
     a) When DB query fails (simulated DB error or mock/exception), sweep is aborted and no files are deleted.
     b) When DB query succeeds: unreferenced files older than 1 hour are deleted, referenced files (by full path or basename) are preserved, and unreferenced files newer than 1 hour are preserved.

WHAT MUST REMAIN UNTOUCHED:
- Do not modify any other files or tables in `lib/core/database/` or `lib/features/`.
</task>

<verification_loop>
Run these commands from `e:\Mohamed\Personal_Project\daily-meal\app_v2` before reporting:
1. `flutter analyze --no-pub` (Must pass with 0 errors, 0 warnings).
2. `flutter test --no-pub test/unit/orphan_image_sweeper_test.dart` (Must pass).
3. `flutter test --no-pub` (All 227+ tests must pass).
</verification_loop>

<action_safety>
Keep changes strictly scoped to:
- `lib/core/services/orphan_image_sweeper.dart`
- `test/unit/orphan_image_sweeper_test.dart`
Do NOT run git add or git commit; the orchestrator reviews and commits. Leave work uncommitted.
</action_safety>

<structured_output_contract>
End your report with:
1. What changed and why
2. Files touched
3. Gate outcomes with counts (flutter analyze, flutter test)
4. Deviations, open items, or decisions needed
</structured_output_contract>
