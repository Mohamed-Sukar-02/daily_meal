# handoff — worker_perf → manager
STATUS: complete. 2 code changes (main.dart cache budget; vault RepaintBoundary ×2 branches),
all inside the "cannot regress behaviour" envelope (no keys, layouts or logic touched).
Font-bundling recommendation escalated to ISSUES.md (needs networked machine).
