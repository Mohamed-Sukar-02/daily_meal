# Drift Schemas

This directory should contain schema dumps generated by:

```
dart run drift_dev schema dump lib/core/database/app_database.dart drift_schemas/
```

Then generate test helpers:

```
dart run drift_dev schema generate drift_schemas/ test/generated_migrations/
```

## Current Status

- `dart` / `flutter` binary is NOT available in this sandbox environment (checked: which dart, which flutter, /opt/flutter missing).
- Therefore schema dumps could NOT be generated automatically as required by task [A].
- The migration test `test/database_migration_test.dart` is written correctly using `SchemaVerifier` + `MigrationsTest` from `drift_dev/api/migrations.dart`.
- It will fail until dumps are generated and `test/generated_migrations/` contains `schema.dart`, `schema_v8.dart`, `schema_v9.dart`, `schema_v10.dart`.
- Once toolchain is available, run the two commands above, then `dart run build_runner build --delete-conflicting-outputs`.

## Expected Versions

- v8: base version on main (before indexes)
- v9: adds indexes idx_meal_history_meal_id, idx_meal_history_cooked_at, idx_meals_name
- v10: adds name_normalized column + idx_meals_name_normalized + batch backfill with throw on failure

This README explicitly declares inability to generate dumps due to missing toolchain, as required by task [A].
