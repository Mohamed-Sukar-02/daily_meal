# BRIEFING — worker_backlog
## Mission
Per plan "Backlog Management": ISSUES.md is the backlog of record. It does not exist in this
repo → create it, reflecting the plan's IMPORTANT note (PopScope marked [x] WITHOUT code
changes), every closed/rejected/open item with evidence pointers, and the plan's verification
checklist honestly annotated (no SDK in sandbox → analyze/test "by construction" + committed
tests to run on an SDK machine).
