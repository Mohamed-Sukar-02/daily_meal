<task>
Fix Settings Reset notification cancellation, language change notification rescheduling, and toggle error rollback in `lib/features/settings/providers/settings_providers.dart` in `e:\Mohamed\Personal_Project\daily-meal\app_v2`.

CURRENT BEHAVIOR:
1. In `SettingsController.resetToDefaults()`:
   ```dart
   final dao = ref.read(appSettingsDaoProvider);
   await dao.resetToDefaults();
   ```
   `dao.resetToDefaults()` sets `notificationsEnabled = false` in SQLite, but `NotificationService.instance.cancelNotification()` is never called. The daily notification reminder remains active in the Android system alarm manager and continues to fire.
2. In `SettingsController.updateLanguage(AppLanguagePreference lang)`:
   Updates SQLite with the new language, but if notifications are enabled, it does NOT reschedule the reminder. The OS notification continues to fire with the previous language's title and body strings.
3. In `SettingsController.toggleNotifications(bool enabled)`:
   If `enabled == true`, it sets `await dao.toggleNotifications(true)` and then calls `scheduleDailyNotification(...)`. If `scheduleDailyNotification` throws an exception (e.g. alarm exception, permission issue), the catch block rethrows without rolling back `dao.toggleNotifications(false)`. The UI switch stays ON even though no reminder is scheduled.

WHAT MUST CHANGE:
1. In `lib/features/settings/providers/settings_providers.dart`:
   - In `resetToDefaults()`:
     Call `await NotificationService.instance.cancelNotification();` in addition to `dao.resetToDefaults()`.
   - In `updateLanguage(AppLanguagePreference lang)`:
     After `await dao.updateLanguage(lang);`, fetch current settings (`final settings = await dao.watchSettings().first;`), and if `settings.notificationsEnabled` is true, reschedule the reminder with the new language strings:
     ```dart
     await NotificationService.instance.scheduleDailyNotification(
       hour: settings.notificationHour,
       minute: settings.notificationMinute,
       strings: _stringsFor(settings),
     );
     ```
   - In `toggleNotifications(bool enabled)`:
     Wrap the scheduling step so that if `scheduleDailyNotification` fails/throws, it calls `await dao.toggleNotifications(false);` and cancels notification before rethrowing or surfacing the error.
2. In tests (`test/unit/settings_notification_sync_test.dart` or new test):
   - Add unit tests verifying:
     a) `resetToDefaults` cancels the notification.
     b) `updateLanguage` reschedules notifications when enabled.
     c) `toggleNotifications` rolls back to false if scheduling throws.

WHAT MUST REMAIN UNTOUCHED:
- Do not modify other settings preferences or theme logic.
</task>

<verification_loop>
Run these commands from `e:\Mohamed\Personal_Project\daily-meal\app_v2` before reporting:
1. `flutter analyze --no-pub` (Must pass with 0 errors, 0 warnings).
2. `flutter test --no-pub test/unit/` (Must pass).
3. `flutter test --no-pub` (All 243+ tests must pass).
</verification_loop>

<action_safety>
Keep changes strictly scoped to:
- `lib/features/settings/providers/settings_providers.dart`
- unit tests under `test/unit/`
Do NOT run git add or git commit; the orchestrator reviews and commits. Leave work uncommitted.
</action_safety>

<structured_output_contract>
End your report with:
1. What changed and why
2. Files touched
3. Gate outcomes with counts (flutter analyze, flutter test)
4. Deviations, open items, or decisions needed
</structured_output_contract>
